const assert=require('assert');const {listFoundingLaunchPortals}=require('../data/foundingLaunchPortals');const {DOMAIN_REGISTRY_SEEDS}=require('../data/domainRegistrySeed');const portals=listFoundingLaunchPortals();assert.strictEqual(portals.length,7);for(const name of ['Divorce and Family Law','Tax Law and Tax Resolution','Immigration Law','Personal Injury Law','Employment Law','Estate and Probate Law','Business and Contract Law'])assert(portals.some(x=>x.name===name),name);const contract=DOMAIN_REGISTRY_SEEDS.find(x=>x.id==='DOMAIN-BUSINESS-CONTRACT-PENDING');assert(contract);assert.strictEqual(contract.ownershipStatus,'domain pending');assert(!DOMAIN_REGISTRY_SEEDS.some(x=>x.domain==='contractcreator.com'&&x.ownershipStatus==='owned'));console.log('Seven-portal founding launch tests passed.');