'use strict';
const assert=require('assert');const fs=require('fs');const path=require('path');const root=path.join(__dirname,'..');for(const f of ['SMARTER_ECOSYSTEM_CONTROL_CENTER_HANDOFF_V0.1.0.json','SMARTER_ECOSYSTEM_CONTROL_CENTER_MIGRATION_BUNDLE_V0.1.0.json','SMARTER_ECOSYSTEM_CONTROL_CENTER_LEARNING_HANDOFF_V1.7.28.json'])assert(!fs.existsSync(path.join(root,f)),f);
const handoff=require('../NEUTRAL_BOARDROOM_HANDOFF_V1.7.33.json');assert.equal(handoff.sourceSystemId,'smarter-justice');assert.equal(handoff.destinationSystemId,'neutral-boardroom-portfolio-os');assert.equal(handoff.liveConnection,false);assert.equal(handoff.automaticWrites,false);for(const key of ['containsUserMatterData','containsCredentials','containsPaymentData','containsConfidentialData'])assert.equal(handoff[key],false,key);assert(handoff.prohibitedData.length>=6);console.log('standalone-ecosystem-handoff-v1722.test.js passed — legacy standalone handoff retired');
