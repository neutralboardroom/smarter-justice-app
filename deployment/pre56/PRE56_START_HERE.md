# Smarter Justice v2.0.0-pre56 — start here

Pre56 is the next qualified source release built from the exact three-file pre55 set restored from ChatGPT Library.

## Material improvement

Pre56 adds a first-party, consent-first acquisition and enrollment measurement lane. Measurement is off by default. After an explicit browser choice and an independent server-side enablement flag, it accepts only allowlisted public paths, actions and acquisition channels. It stores daily aggregate counts for a bounded 30-day window and does not store legal narratives, search terms, query strings, referrers, names, emails, IP addresses in measurement rows, user agents, fingerprints, cookies, device identifiers or session identifiers. Ratios are withheld below a 20-count denominator and are explicitly non-cohort directional indicators.

The public privacy-choice page is `/measurement-privacy.html`. The owner-only aggregate view is `/api/owner/private-acquisition-measurement`. No third-party analytics script is added.

## Preserved boundaries

- The story-first public entry, Navigator, attorney demonstration, responsive navigation, bilingual access and established navy/teal/mint/gold visual system remain intact.
- `/growth-operations-compliance.html` and `/portals.html` remain ordinary-public 404 routes and remain absent from the sitemap.
- The qualified public directory baseline remains 25 professionals and 5 firms. The protected 30-record currentness queue is carried forward with zero new accepted profiles.
- Pre56 does not open payment, publication, outreach, external consequential actions or automatic overage billing.
- The accepted-live rollback identity remains pre54 because no newer complete accepted-live proof is present in the build authority.

## Deployment truth

This package is qualified source, not proof of a live deployment. Owner deployment authorization for pre56 is false. Any later deployment requires a new exact owner command, successful qualification for the exact commit, protected provider credentials, exact provider terminal-live proof, complete public acceptance and automatic rollback to the accepted-live pre54 deployment on failure.

Read next:

1. `PRIVATE_ACQUISITION_MEASUREMENT__PRE56.json`
2. `LIVE_STATE_AND_DEPLOYMENT_BOUNDARY__PRE56.json`
3. `OWNER_DEPLOYMENT_AUTHORIZATION__PRE56.json`
4. `PROFILE_CURRENTNESS_WORK_QUEUE__PRE56.json`
5. `UNIQUE_UX_UI_REVIEW__PRE56.json`
6. `RESEARCH_REGISTER__PRE56.json`
7. `NEXT_VERSION_IMPROVEMENT_LIST__PRE56.md`
