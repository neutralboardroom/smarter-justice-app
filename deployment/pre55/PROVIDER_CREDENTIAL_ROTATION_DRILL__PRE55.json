{
  "schemaVersion": "1.0.0",
  "release": "v2.0.0-pre55",
  "builder": "J44",
  "slice": "PROTECTED_TIME_BOUNDED_PROVIDER_CREDENTIAL_ROTATION_DRILL",
  "predecessor": "v2.0.0-pre54",
  "renderService": {
    "id": "srv-d8ps9jgjs32c73918vvg",
    "repository": "neutralboardroom/smarter-justice-app",
    "branch": "main",
    "autoDeploy": "off"
  },
  "maximumWindowMinutes": 60,
  "phases": [
    {
      "id": "candidate-access",
      "expected": "Candidate credential can read the exact protected service and Auto-Deploy remains off."
    },
    {
      "id": "retired-denied",
      "expected": "The manually revoked predecessor credential receives HTTP 401 or 403."
    },
    {
      "id": "active-access",
      "expected": "The final active credential can read the exact protected service and Auto-Deploy remains off."
    }
  ],
  "manualProviderActions": [
    "Create the candidate credential in Render Account Settings.",
    "Store candidate and predecessor credentials only in protected production-environment secrets.",
    "After candidate verification, update the active automation secret and revoke the predecessor in Render.",
    "After denial and active verification, remove temporary candidate and retired secret slots."
  ],
  "forbiddenEvidence": [
    "credential value",
    "credential hash or fingerprint",
    "authorization header",
    "private deploy-hook URL",
    "provider response body containing unexpected private data"
  ],
  "failurePolicy": "Fail closed on expired window, wrong service, Auto-Deploy enabled, candidate or active denial, retired access, unknown status or secret serialization.",
  "productionMutationByDrillScript": false,
  "publicationImpact": "SEO sitemap continuity only; no visual UI or new profile publication"
}
