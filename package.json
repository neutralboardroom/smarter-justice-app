{
  "name": "smarter-justice-pre127-render-bootstrap",
  "version": "2.0.0-pre127",
  "private": true,
  "description": "Compact deployment bridge that reconstructs exact PRE126 production and applies the qualified Smarter Justice working professional-community release.",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "nodemailer": "^9.0.3",
    "pg": "^8.13.1"
  },
  "scripts": {
    "postinstall": "node tests/pre127-render-filesystem.test.js && node scripts/bootstrap-pre127-release.js",
    "start": "node scripts/start-pre127-deploy.js",
    "test": "npm --prefix .runtime/pre127-live test",
    "qualify:pre127": "node scripts/bootstrap-pre127-release.js && npm --prefix .runtime/pre127-live run qualify:pre127",
    "deployment:validate": "npm --prefix .runtime/pre127-live run deployment:validate",
    "deployment:doctor": "npm --prefix .runtime/pre127-live run deployment:doctor"
  }
}
