{
  "name": "smarter-justice-pre128-render-bootstrap",
  "version": "2.0.0-pre128",
  "private": true,
  "description": "Compact deployment bridge that reconstructs exact PRE127 production and applies the qualified truthful Smarter Justice universal successor.",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "nodemailer": "^9.0.3",
    "pg": "^8.13.1"
  },
  "scripts": {
    "postinstall": "node tests/pre128-render-filesystem.test.js && node scripts/bootstrap-pre128-release.js",
    "start": "node scripts/start-pre128-deploy.js",
    "migrate": "node scripts/migrate-pre128-deploy.js",
    "test": "npm --prefix .runtime/pre128-live test",
    "qualify:pre128": "node scripts/bootstrap-pre128-release.js && npm --prefix .runtime/pre128-live run qualify:pre128",
    "deployment:validate": "npm --prefix .runtime/pre128-live run deployment:validate",
    "deployment:doctor": "npm --prefix .runtime/pre128-live run deployment:doctor"
  }
}
