name: Archived PRE55 deployment workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archive status
        run: |
          echo "::notice::PRE55 deployment automation is archived."
          echo "Use the current PRE124 qualification workflows and the protected Render deployment path."
