(()=>{const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function publicLabel(value){const v=String(value||'').toLowerCase();if(v.includes('live')||v.includes('available'))return 'Available now';if(v.includes('development')||v.includes('preparation')||v.includes('testing')||v.includes('staging'))return 'Being prepared';if(v.includes('pilot paused')||v.includes('paused'))return 'Not available yet';if(v.includes('applications'))return 'Enrollment availability varies';if(v.includes('controlled pilot'))return 'Limited availability';if(v.includes('planned'))return 'Planned';return String(value||'Availability varies');}function pill(v){return `<span class="status-pill">${esc(publicLabel(v))}</span>`;}async function load(){const grid=document.getElementById('foundingPortalGrid');try{const data=await fetch('/api/founding-launch-portals').then(r=>r.json());if(!data.ok)throw new Error(data.error||'Could not load the priority areas.');grid.innerHTML=data.portals.map(p=>`<article class="card founding-portal-card"><div class="founding-portal-card-head"><div><p class="eyebrow">${esc(p.brandName)}</p><h2>${esc(p.name)}</h2><p class="domain-line">${p.officialDomain?esc(p.officialDomain):'Focused website name coming soon'}</p></div>${pill(p.domainStatus)}</div><div class="portal-status-stack">${pill(p.publicStage)}${pill(p.professionalStage)}</div><h3>Public starting paths</h3><ul>${p.publicWorkflows.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><h3>Professional participation</h3><ul>${p.professionalValue.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><details><summary>Professional types and important limits</summary><p><strong>Professional types:</strong> ${esc(p.professionalTypes.join(', '))}</p><ul>${p.limitations.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></details><div class="card-actions">${p.externalUrl?`<a class="secondary button-link" href="${esc(p.externalUrl)}" rel="noopener">Visit Separate Platform</a>`:`<a class="secondary button-link" href="${esc(p.publicPage)}">Open Starting Page</a>`}<a class="primary button-link" href="/professional-signup.html?portal=${encodeURIComponent(p.portfolioSlug)}">Prepare Professional Account</a></div></article>`).join('');}catch(err){grid.innerHTML=`<p class="error">${esc(err.message)}</p>`;}}document.addEventListener('DOMContentLoaded',load);})();