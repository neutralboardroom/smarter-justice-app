#!/usr/bin/env node
'use strict';const fs=require('fs');const path=require('path');const readiness=require('../lib/deploymentReadiness');const root=path.join(__dirname,'..');const out=readiness.validate(root);const render=fs.readFileSync(path.join(root,'render.yaml'),'utf8');const workflow=fs.readFileSync(path.join(root,'.github','workflows','production-qualification.yml'),'utf8');const errors=[...out.errors];if(!render.includes('healthCheckPath: /livez'))errors.push('render-liveness-path');if(!workflow.includes('qualification-gate'))errors.push('qualification-gate');if(!workflow.includes('npm run deployment:validate'))errors.push('workflow-deployment-validation');const result={command:'deployment:validate',ok:errors.length===0,errors,releaseVersion:out.releaseVersion,requiredFileCount:readiness.requiredFiles.length};process.stdout.write(JSON.stringify(result,null,2)+'\n');if(!result.ok)process.exitCode=1;
