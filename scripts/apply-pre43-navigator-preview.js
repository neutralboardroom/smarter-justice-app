'use strict';

const fs=require('node:fs');
const path=require('node:path');

const root=path.resolve(process.argv[2]||'.');
const serverPath=path.join(root,'server.js');
const publicDir=path.join(root,'public');
const indexPath=path.join(publicDir,'index.html');
const marker='SMARTER_JUSTICE_PRE43_PUBLIC_NAVIGATOR';

function fail(message){throw new Error(`[pre43-public-navigator] ${message}`);}
function read(file){if(!fs.existsSync(file))fail(`missing ${path.relative(root,file)}`);return fs.readFileSync(file,'utf8');}
function write(file,content){fs.writeFileSync(file,content,'utf8');}

let server=read(serverPath);
if(server.includes(marker))fail('server overlay already applied');

const topNeedle="const http = require('http');";
if(!server.includes(topNeedle))fail('server http import seam missing');
server=server.replace(topNeedle,`${topNeedle}\nconst { spawn } = require('child_process'); // ${marker}`);

const helperNeedle='function json(res, status, data){';
if(!server.includes(helperNeedle))fail('json helper seam missing');
const helper=`// ${marker}\nconst PRE43_NAVIGATOR_COOKIE='sj_nav';\nfunction pre43StableHash(prefix,value){return prefix+crypto.createHash('sha256').update(String(value||'')).digest('hex').slice(0,40);}\nfunction pre43NavigatorIdentity(req){\n  const owner=ownerAccounts.accountFromRequest(req);\n  if(owner&&owner.account){const a=owner.account||{};const raw=a.id||a.accountId||a.email||'owner';return{actorRef:pre43StableHash('owner:',raw),actorClass:'OWNER',cookie:null};}\n  const professional=professionalAccounts.accountFromRequest(req);\n  if(professional&&professional.account){const a=professional.account||{};const raw=a.id||a.accountId||a.email||'professional';return{actorRef:pre43StableHash('professional:',raw),actorClass:'PROFESSIONAL',cookie:null};}\n  const cookies=security.parseCookies(req);\n  let token=String(cookies[PRE43_NAVIGATOR_COOKIE]||'').trim();\n  let cookie=null;\n  if(!/^[A-Za-z0-9_-]{24,80}$/.test(token)){token=crypto.randomBytes(32).toString('base64url');cookie=security.cookie(PRE43_NAVIGATOR_COOKIE,token,{maxAgeSeconds:30*86400,sameSite:'Lax',httpOnly:true});}\n  const actorClass=staffSessionValid(req)?'STAFF':'PUBLIC_USER';\n  return{actorRef:pre43StableHash(actorClass==='STAFF'?'staff:':'public:',token),actorClass,cookie};\n}\nfunction pre43NavigatorCli(payload){\n  return new Promise((resolve,reject)=>{\n    const script=path.resolve(process.cwd(),'deployment/pre43/navigator_preview_cli.py');\n    const child=spawn(process.env.PYTHON_BIN||'python3',[script],{cwd:process.cwd(),env:{...process.env},stdio:['pipe','pipe','pipe']});\n    let out='',err='';\n    const timer=setTimeout(()=>{child.kill('SIGKILL');reject(new Error('Navigator timed out.'));},50000);\n    child.stdout.on('data',chunk=>{out+=String(chunk);if(out.length>1024*1024)child.kill('SIGKILL');});\n    child.stderr.on('data',chunk=>{err+=String(chunk);if(err.length>128*1024)child.kill('SIGKILL');});\n    child.on('error',error=>{clearTimeout(timer);reject(error);});\n    child.on('close',code=>{\n      clearTimeout(timer);\n      let parsed;try{parsed=JSON.parse(out||'{}');}catch{parsed={ok:false,errorCode:'NAVIGATOR_BRIDGE_ERROR',message:'Navigator returned an unreadable response.'};}\n      if(code!==0&&!parsed.message)parsed.message='Navigator could not complete this request.';\n      resolve(parsed);\n    });\n    child.stdin.end(JSON.stringify(payload));\n  });\n}\nfunction pre43NavigatorJson(res,status,payload,identity){return identity&&identity.cookie?jsonWithCookie(res,status,payload,identity.cookie):json(res,status,payload);}\n\n`;
server=server.replace(helperNeedle,helper+helperNeedle);

const apiNeedle="  if (req.method === 'GET' && pathName === '/api/owner/control-center') {";
if(!server.includes(apiNeedle))fail('API insertion seam missing');
const apiRoutes=`  // ${marker}\n  if (req.method === 'GET' && (pathName === '/api/navigator/status' || pathName === '/api/ai/universal/status')) {\n    const identity=pre43NavigatorIdentity(req);\n    const result=await pre43NavigatorCli({action:'status',actorRef:identity.actorRef,actorClass:identity.actorClass});\n    return pre43NavigatorJson(res,result.ok?200:503,{...result,version:'v2.0.0-pre43',navigatorAvailable:true,publicAiEnabled:true,externalConsequentialActionsEnabled:false},identity);\n  }\n  if (req.method === 'POST' && (pathName === '/api/navigator/chat' || pathName === '/api/ai/universal')) {\n    const identity=pre43NavigatorIdentity(req);\n    const maxRequests=identity.actorClass==='OWNER'?120:identity.actorClass==='STAFF'?90:identity.actorClass==='PROFESSIONAL'?75:30;\n    const limited=await rateLimit(req,'pre43-navigator-'+identity.actorClass.toLowerCase(),{maxRequests,windowMs:60*60*1000});\n    if(limited)return pre43NavigatorJson(res,429,{ok:false,error:'Navigator request limit reached. Try again later.',retryAfterSeconds:limited.retryAfterSeconds},identity);\n    const body=await parseJson(req);\n    const result=await pre43NavigatorCli({action:'chat',actorRef:identity.actorRef,actorClass:identity.actorClass,threadId:String(body.threadId||''),message:String(body.message||''),inputMode:String(body.inputMode||'TEXT'),toolMode:String(body.toolMode||'NAVIGATOR')});\n    const status=result.ok?200:(result.errorCode==='THREAD_ACCESS_DENIED'?403:result.errorCode==='AI_ALLOWANCE_BLOCKED'?429:502);\n    return pre43NavigatorJson(res,status,{...result,version:'v2.0.0-pre43',navigatorAvailable:true,publicAiEnabled:true,externalConsequentialActionsEnabled:false},identity);\n  }\n\n`;
server=server.replace(apiNeedle,apiRoutes+apiNeedle);
write(serverPath,server);

const html=`<!DOCTYPE html>\n<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#123d5f"><link rel="icon" href="/favicon.svg" type="image/svg+xml"><link rel="stylesheet" href="/styles.css"><script defer src="/app.js"></script><script defer src="/navigator.js"></script><title>Navigator | Smarter Justice</title><meta name="description" content="Smarter Justice Navigator helps public users, attorneys and law firms organize questions, draft, summarize and identify next steps with AI assistance."><style>\n.navp-shell{max-width:1040px;margin:0 auto;padding:28px 20px 64px}.navp-hero{padding:30px 0 18px;border-bottom:1px solid #d0d5dd}.navp-hero h1{font-size:clamp(38px,6vw,58px);margin:.15em 0}.navp-hero p{max-width:820px;line-height:1.65;color:#475467}.navp-status{display:flex;gap:10px;flex-wrap:wrap;margin:18px 0}.navp-status span{border:1px solid #d0d5dd;border-radius:6px;padding:6px 9px;background:#fff;font-size:13px}.navp-tools{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:18px 0}.navp-tool{border:1px solid #d0d5dd;border-radius:8px;background:#fff;padding:11px;text-align:left;font-weight:700}.navp-tool[aria-pressed="true"]{border-color:#174ea6;outline:2px solid #174ea633}.navp-panel{border:1px solid #d0d5dd;border-radius:10px;background:#fff;padding:18px;margin-top:18px}.navp-chat{display:flex;flex-direction:column;gap:12px;min-height:300px;max-height:56vh;overflow:auto;padding:4px}.navp-msg{max-width:82%;border:1px solid #d0d5dd;border-radius:10px;padding:12px 14px;white-space:pre-wrap;line-height:1.55}.navp-msg.user{align-self:flex-end;background:#f2f4f7}.navp-msg.assistant{align-self:flex-start;background:#fff}.navp-form textarea{width:100%;min-height:112px;resize:vertical}.navp-actions{display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-top:10px}.navp-result{color:#475467;font-size:14px}.navp-note{margin-top:18px;padding:14px 16px;border-left:4px solid #174ea6;background:#f8fafc;color:#475467;line-height:1.55}.navp-home-cta{border:1px solid #d0d5dd;border-radius:12px;background:#fff;padding:20px;margin:20px auto;max-width:1180px}.navp-home-cta h2{margin-top:0}.navp-home-cta a{display:inline-block;margin-top:8px;font-weight:800}@media(max-width:760px){.navp-tools{grid-template-columns:1fr 1fr}.navp-msg{max-width:94%}}@media(max-width:500px){.navp-tools{grid-template-columns:1fr}}\n</style></head><body><a class="skip" href="#main">Skip to content</a><header class="site-header"><a class="brand" href="/"><img src="/logo.svg" alt="Smarter Justice"></a><nav><a href="/">Home</a><a href="/professionals.html">For professionals</a></nav></header><main id="main" class="navp-shell"><section class="navp-hero"><p class="eyebrow">Smarter Justice AI · v2.0.0-pre43</p><h1>Navigator</h1><p>Type or dictate what you want help with. Navigator can explain, organize, draft, summarize, create checklists, and help attorneys and firms work through Smarter Justice. Public AI assistance is live. Actions that affect money, filings, accounts, publication, communications, or legal rights still require the proper authorization and confirmation.</p><div class="navp-status" id="navigatorStatus"><span>Checking AI…</span><span>Available to everyone</span><span>External actions confirmation-gated</span></div></section><section aria-labelledby="toolHeading"><h2 id="toolHeading">Choose an AI tool</h2><div class="navp-tools" id="navigatorTools"><button class="navp-tool" data-mode="NAVIGATOR" aria-pressed="true">Navigator</button><button class="navp-tool" data-mode="STARTING_POINT" aria-pressed="false">Starting Point</button><button class="navp-tool" data-mode="DRAFT" aria-pressed="false">Draft</button><button class="navp-tool" data-mode="SUMMARIZE" aria-pressed="false">Summarize</button><button class="navp-tool" data-mode="CHECKLIST" aria-pressed="false">Checklist</button><button class="navp-tool" data-mode="PROFESSIONAL_WORKFLOW" aria-pressed="false">Professional Workflow</button></div></section><section class="navp-panel"><div class="navp-chat" id="navigatorChat" aria-live="polite"><div class="navp-msg assistant">Hi. I’m Navigator. Tell me what you are trying to do in plain language. I can help you work through it.</div></div><form class="navp-form" id="navigatorForm"><label for="navigatorMessage"><strong>Message Navigator</strong></label><textarea id="navigatorMessage" name="message" maxlength="12000" required placeholder="Type what you want help with…"></textarea><div class="navp-actions"><button class="primary" type="submit">Send</button><button type="button" id="navigatorDictate">Dictate</button><button type="button" id="navigatorNewThread">New conversation</button><span class="navp-result" id="navigatorResult"></span></div></form></section><aside class="navp-note"><strong>Important:</strong> Smarter Justice is not a law firm or government agency. Navigator can help prepare and organize work, but it does not create an attorney-client relationship or guarantee outcomes. Do not enter passwords, secret keys, full Social Security numbers, or full financial account numbers.</aside></main><footer class="site-footer"><p><strong>Smarter Justice</strong> · AI assistance for the public and legal professionals.</p></footer></body></html>\n`;
write(path.join(publicDir,'navigator.html'),html);

const js=`'use strict';\n(()=>{\n  const $=s=>document.querySelector(s),$$=s=>Array.from(document.querySelectorAll(s));\n  const chat=$('#navigatorChat'),form=$('#navigatorForm'),message=$('#navigatorMessage'),result=$('#navigatorResult'),status=$('#navigatorStatus'),newThread=$('#navigatorNewThread'),dictate=$('#navigatorDictate');\n  if(!form)return;\n  let threadId=sessionStorage.getItem('sjNavigatorThread')||'';\n  let toolMode=sessionStorage.getItem('sjNavigatorMode')||'NAVIGATOR';\n  const add=(role,text)=>{const el=document.createElement('div');el.className='navp-msg '+role;el.textContent=String(text||'');chat.appendChild(el);chat.scrollTop=chat.scrollHeight;};\n  const setStatus=(text,error=false)=>{result.textContent=text||'';result.style.color=error?'#b42318':'';};\n  function selectMode(mode){toolMode=mode;sessionStorage.setItem('sjNavigatorMode',mode);$$('[data-mode]').forEach(b=>b.setAttribute('aria-pressed',b.dataset.mode===mode?'true':'false'));}\n  $$('[data-mode]').forEach(b=>b.addEventListener('click',()=>selectMode(b.dataset.mode)));selectMode(toolMode);\n  async function loadStatus(){\n    try{const r=await fetch('/api/navigator/status');const x=await r.json();if(!r.ok)throw new Error(x.error||x.message||'AI unavailable');const p=x.providerReadiness||{};status.innerHTML='';[['AI',p.ready?'OpenAI ready':'Not ready'],['Access','Everyone'],['User class',x.actorClass||'PUBLIC_USER'],['Consequential actions','Confirmation-gated']].forEach(([a,b])=>{const s=document.createElement('span');s.textContent=a+': '+b;status.appendChild(s);});}\n    catch(e){status.textContent='Navigator status unavailable: '+e.message;}\n  }\n  form.addEventListener('submit',async e=>{\n    e.preventDefault();const text=message.value.trim();if(!text)return;add('user',text);message.value='';setStatus('Navigator is thinking…');form.querySelector('button[type="submit"]').disabled=true;\n    try{const r=await fetch('/api/navigator/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({threadId,message:text,inputMode:'TEXT',toolMode})});const x=await r.json();if(!r.ok||!x.ok)throw new Error(x.message||x.error||'Navigator request failed');threadId=x.threadId||threadId;if(threadId)sessionStorage.setItem('sjNavigatorThread',threadId);add('assistant',x.answer);setStatus('AI response received · usage recorded');}\n    catch(err){add('assistant','I could not complete that request. '+err.message);setStatus(err.message,true);}\n    finally{form.querySelector('button[type="submit"]').disabled=false;message.focus();}\n  });\n  newThread.addEventListener('click',()=>{threadId='';sessionStorage.removeItem('sjNavigatorThread');chat.innerHTML='';add('assistant','New Navigator conversation started.');message.focus();});\n  const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;\n  if(!Recognition){dictate.hidden=true;}else{dictate.addEventListener('click',()=>{const r=new Recognition();r.lang=document.documentElement.lang==='es'?'es-US':'en-US';r.interimResults=false;r.maxAlternatives=1;setStatus('Listening…');r.onresult=e=>{const spoken=e.results?.[0]?.[0]?.transcript||'';message.value=(message.value+' '+spoken).trim();setStatus('Dictation added. Review it before sending.');message.focus();};r.onerror=()=>setStatus('Dictation was not available. You can keep typing.',true);r.start();});}\n  loadStatus();\n})();\n`;
write(path.join(publicDir,'navigator.js'),js);

let index=read(indexPath);
if(!index.includes('SMARTER_JUSTICE_PRE43_NAVIGATOR_HOME_CTA')){
  const cta=`<!-- SMARTER_JUSTICE_PRE43_NAVIGATOR_HOME_CTA --><section class="navp-home-cta"><p class="eyebrow">Smarter Justice AI</p><h2>Tell Navigator what you want to do.</h2><p>Public users, attorneys and law firms can now use Smarter Justice AI to organize questions, draft, summarize, make checklists and identify next steps.</p><a href="/navigator">Open Navigator →</a></section>`;
  const bodyMatch=index.match(/<main[^>]*>/i);
  if(bodyMatch)index=index.replace(bodyMatch[0],bodyMatch[0]+cta);else index=index.replace(/<body[^>]*>/i,m=>m+cta);
  if(!index.includes('.navp-home-cta{'))index=index.replace(/<\/head>/i,'<style>.navp-home-cta{border:1px solid #d0d5dd;border-radius:12px;background:#fff;padding:20px;margin:20px auto;max-width:1180px}.navp-home-cta h2{margin-top:0}.navp-home-cta a{font-weight:800}</style></head>');
  write(indexPath,index);
}
console.log('[pre43-public-navigator] public Navigator, AI tool modes, dictation adapter, usage controls, and homepage entry applied');
